#include "main.h"
/**
 * _abs - is a function for int
 * @n: is an integer still.
 * Return: not sure what to put here.
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
	return (0);
}
