#include <stdio.h>
/**
 * main - is our programmes entry point
 * Return: 0 value is a sign of success
 */
int main(void)
{
	char a;

	for (a = 'a'; a <= 'z' ; a++)
		putchar(a);
	putchar('\n');
	return (0);
}
