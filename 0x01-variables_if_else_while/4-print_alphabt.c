#include <stdio.h>


/**
 * main  - prints the lettersof alphabets
 *
 * return  0
 */

int main (void)
{
/*Declarations*/
	char letter;
	for (letter = 'a'; letter <= 'z'; letter++)
	{
	if (letter != 'e' && letter != 'q')
	}
}
