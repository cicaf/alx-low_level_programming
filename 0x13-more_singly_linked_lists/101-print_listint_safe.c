#include <stdlib.h>
#include <stdio.h>
/**
 * print_listint_safe - linked list.
 * @head: chwakis.
 *
 * Return: total nodes
 */
size_t print_listint_safe(const listint_t *head)
{

	}
	free_aux(a);
	return (c);
