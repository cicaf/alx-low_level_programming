#include "main.h"
#include <stdio.h>

/**
  * main - Print the name
  * @argc: arg count
  * @argv: arg vector
  *
  * Return: returns 0 as usual
  */
int main(int argc, char *argv[])
{
	(void) argc;
	printf("%s\n", argv[0]);

	return (0);
}
